import { NgModule } from '@angular/core';
import { BlockuiLoadingComponent } from './blockui-loading.component';

@NgModule({
  declarations: [BlockuiLoadingComponent],
  exports: [BlockuiLoadingComponent],
})
export class BlockuiLoadingModule { }
